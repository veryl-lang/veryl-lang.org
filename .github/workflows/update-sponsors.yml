name: Update Sponsors

on:
  workflow_dispatch:
  # Re-enable the daily schedule once SPONSORKIT_OPENCOLLECTIVE_KEY is set as a
  # repository secret (and there are backers to show).
  # schedule:
  #   - cron: '0 0 * * *'  # daily at 00:00 UTC

permissions:
  contents: write
  actions: write

concurrency:
  group: update-sponsors
  cancel-in-progress: true

jobs:
  update-sponsors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Generate sponsors image (Open Collective)
        # Install sponsorkit locally first: sponsor.config.ts does
        # `import ... from 'sponsorkit'`, which a bare `npx sponsorkit` cannot
        # resolve from the project directory.
        run: |
          npm install sponsorkit@latest
          # Tolerate a sponsorkit / Open Collective failure (e.g. no contributions
          # yet, which currently makes the OpenCollective provider throw): keep the
          # existing image instead of failing the job.
          npx sponsorkit || echo "::warning title=sponsorkit::sponsorkit failed; keeping the current sponsors image"
        env:
          # PAT with `read:user` and `read:org` scopes.
          SPONSORKIT_GITHUB_TOKEN: ${{ secrets.SPONSORKIT_GITHUB_TOKEN }}
          # Open Collective personal token (Settings > For developers > Personal tokens)
          SPONSORKIT_OPENCOLLECTIVE_KEY: ${{ secrets.SPONSORKIT_OPENCOLLECTIVE_KEY }}
          SPONSORKIT_OPENCOLLECTIVE_SLUG: veryl

      - name: Keep the placeholder when there are no sponsors
        # sponsorkit embeds each avatar as an <image> element; with no sponsors
        # the generated SVG has none, so restore the committed placeholder.
        run: |
          if ! grep -q '<image' static/img/sponsors.svg; then
            echo "No sponsors found — keeping the placeholder."
            git checkout -- static/img/sponsors.svg
          fi

      - name: Commit updated image
        id: commit
        uses: EndBug/add-and-commit@v9
        with:
          message: 'chore: update sponsors.svg [skip ci]'
          add: 'static/img/sponsors.svg'
          default_author: github_actions

      # The image is served first-party from static/img. A bot commit made with
      # GITHUB_TOKEN does not trigger the push-based Deploy workflow, so dispatch
      # it explicitly (workflow_dispatch is allowed with GITHUB_TOKEN).
      - name: Trigger site deploy if the image changed
        if: steps.commit.outputs.committed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run deploy.yml --ref main
